# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules/
/.pnp
.pnp.js

# testing
/coverage

# next.js
.next/
web/out/

# build output generated by `npm run prep` — regenerated on every deploy, never committed
web/public/data/
web/public/downloads/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# AI / assistant local state
.claude/

# R artifacts — never committed. legacy/app_MAIN.R is kept for provenance, but the .rds
# snapshot is not: data/portfolio.csv is the committed source of truth (see spec.md §3.1).
*.rds
